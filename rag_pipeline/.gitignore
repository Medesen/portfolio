# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
logs/
*.log

# =============================================================================
# Data Directories
# =============================================================================
# IMPORTANT: Only track source corpus, exclude all generated/processed data
# 
# TRACKED (in Git):
#   - data/corpus/scikit-learn-1.7.2-docs/  (source documentation)
#
# NOT TRACKED (generated by pipeline):
#   - data/processed/       (generated by preprocessing)
#   - data/state/          (runtime state tracking)
#   - data/vector_store/   (ChromaDB embeddings ~15-20 MB)
#   - data/evaluation/     (evaluation results)
# =============================================================================

# Exclude all processed data (generated by preprocessing step)
data/processed/

# Exclude state tracking files (generated at runtime)
data/state/

# Exclude vector databases and embeddings (generated by indexing step)
# This can be 15-20 MB for the indexed embeddings
data/vector_store/
data/embeddings/

# Exclude evaluation outputs (generated by evaluation step)
data/evaluation/results/
data/evaluation/test_sets/

# Note: data/corpus/scikit-learn-1.7.2-docs/ IS tracked (source data for preprocessing)

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Testing
.pytest_cache/
.coverage
htmlcov/

# Temporary files
*.tmp
*.bak

# =============================================================================
# Downloaded Models (NOT in repo - stored in user's home directory)
# =============================================================================
# The ~8 GB of models are stored outside the repository:
#   - Sentence-transformers: ~/.cache/torch/sentence_transformers/
#   - Ollama models: ~/.ollama/models/
#
# In case any models accidentally get downloaded to project dir, exclude them:
*.bin
*.safetensors
*.onnx
*.pt
*.pth
models/
model_cache/
.cache/

# Exclude any accidentally downloaded model files
*.model
tokenizer_config.json
# config.json  # Commented out - we DO want config/config.yaml tracked
special_tokens_map.json
vocab.txt
sentencepiece.bpe.model

# =============================================================================
# Docker
# =============================================================================
# These are tracked (part of the Docker setup):
#   - Dockerfile
#   - docker-compose.yml
#   - .dockerignore
#
# Don't commit test output or temporary Docker files:
.docker/
docker-compose.override.yml
*.dockerignore.bak
SUMMARY.txt
