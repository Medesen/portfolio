# Named experiment: slower learning rate, more boosting rounds.
#
# Usage:
#   mlctl train --config-name=gbm_tuned
defaults:
  - train
  - _self_

experiment_name: gbm_tuned

model:
  learning_rate: 0.05
  max_iter: 500
